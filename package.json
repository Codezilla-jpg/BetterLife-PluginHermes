{
  "name": "betterlife-plugin-hermes",
  "version": "0.6.1",
  "private": true,
  "description": "Hermes Desktop limits pane, local clock and Client/Gateway/Hermes restart actions",
  "type": "module",
  "scripts": {
    "check": "node --check plugin.js && python3 -m py_compile backend/dashboard/plugin_api.py",
    "test": "node --experimental-vm-modules tests/smoke.mjs && python3 -m unittest tests/test_backend.py",
    "package": "python3 scripts/package.py",
    "verify": "npm run check && npm test && npm run package && cd dist && sha256sum -c betterlife-plugin-hermes-v0.6.1.zip.sha256"
  },
  "engines": {
    "node": ">=22 <25"
  }
}
