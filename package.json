{
  "name": "hermes-statusline-workspaces",
  "version": "0.1.0",
  "private": true,
  "description": "Scaffold for Hermes Desktop statusline and workspace contributions",
  "type": "module",
  "scripts": {
    "check": "node --check plugin.js",
    "test": "node --experimental-vm-modules tests/smoke.mjs",
    "package": "python3 scripts/package.py",
    "verify": "npm run check && npm test && npm run package"
  },
  "engines": {
    "node": ">=22 <25"
  }
}
