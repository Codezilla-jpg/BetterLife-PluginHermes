{
  "name": "hermes-statusline-workspaces",
  "version": "0.2.0",
  "private": true,
  "description": "Personal Hermes Desktop statusline for account usage, context and local time",
  "type": "module",
  "scripts": {
    "check": "node --check plugin.js",
    "test": "node --experimental-vm-modules tests/smoke.mjs",
    "package": "python3 scripts/package.py",
    "verify": "npm run check && npm test && npm run package && cd dist && sha256sum -c statusline-workspaces-hermes-desktop-v0.2.0.zip.sha256"
  },
  "engines": {
    "node": ">=22 <25"
  }
}
